services:
  app:
    build:
      context: ..
      dockerfile: .infra/Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgres://user:password@db:5432/starve_db
      - RUST_LOG=info

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: starve_db
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
