# Systems Module

Pure gameplay logic and rules.

## Responsibilities
- Survival mechanics (hunger, temperature).
- Crafting logic (recipes, inputs/outputs).
- Combat resolution.
- Spawning and AI behavior rules.
- Interaction handling.

## Dependencies
- `world` (to read/write entity state).
- `config` (for game rules).
