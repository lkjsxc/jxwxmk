# Config Loader

Loads JSON configuration files from `config/` with defaults.

## Contents

- `mod.rs`: config aggregation types.
- `loader.rs`: filesystem loading and parsing.
- `defaults.rs`: default values for missing files.
- `files/`: per-file schema types.
