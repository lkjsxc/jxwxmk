{
  "name": "starve-client",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc && mkdir -p dist && cp *.html *.css dist/ 2>/dev/null || true"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}