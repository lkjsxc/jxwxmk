{
  "name": "jxwxmk-client",
  "version": "0.1.0",
  "description": "JXWXMK game client",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=../static/game.js --target=es2020",
    "watch": "esbuild index.ts --bundle --outfile=../static/game.js --target=es2020 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  }
}
