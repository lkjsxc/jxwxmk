{
  "name": "jxwxmk-client",
  "version": "0.1.0",
  "description": "JXWXMK TypeScript client",
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=../static/game.js --format=iife --target=es2020",
    "watch": "esbuild index.ts --bundle --outfile=../static/game.js --format=iife --target=es2020 --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.2.0"
  }
}
