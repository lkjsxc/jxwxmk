{
  "name": "kkmypk-client",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=../../static/game.js --minify --target=es2020 --platform=browser",
    "watch": "esbuild index.ts --bundle --outfile=../../static/game.js --watch --target=es2020 --platform=browser"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
