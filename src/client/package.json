{
  "name": "starve-client",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=../static/game.js --minify",
    "watch": "esbuild index.ts --bundle --outfile=../static/game.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
