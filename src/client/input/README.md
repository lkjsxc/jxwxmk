# Input

Input handling system.

- `keyboard.ts` - Keyboard input
- `touch.ts` - Touch input with joystick and gestures
- `mouse.ts` - Mouse input with long-press detection
- `manager.ts` - Unified input manager that combines all sources
