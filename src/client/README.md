# Client Source

TypeScript source code for the browser game client.

## Build
Built via `esbuild`. See `../technical/frontend/build.md`.

## Structure
- `index.ts`: Entrypoint.
- `input/`: Input handling.
- `rendering/`: Canvas 2D renderer.
- `ui/`: DOM-based UI overlay.
