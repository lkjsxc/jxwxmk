# Engine Events

Queued commands from IO handlers to the tick-owned `GameEngine`.

## Contents

- `input.rs`: movement + action input state.
- `requests.rs`: craft, trade, quest, spawn, and session events.
