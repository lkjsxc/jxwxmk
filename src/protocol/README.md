# Protocol Module

Defines the core data types for Client-Server communication.

## Responsibilities
- Protocol message definitions (Enums/Structs).
- JSON serialization/deserialization.
- Input validation (bounds, lengths).
- Shared domain identifiers (newtypes).

## Dependencies
- **None** (pure data types).
