# Rust Server Crate

Authoritative simulation, HTTP/WebSocket endpoints, persistence, and asset serving.

## Contents

- `Cargo.toml`: Server crate manifest.
- `src/`: Server source code.
- `tests/`: Integration tests (handshake/auth, DB init).
