# World Module

Manages the spatial representation of the game world.

## Responsibilities
- Chunk data structure and storage.
- Spatial index (entity lookup by position).
- Pathfinding / Collision helpers.
- World state container.

## Dependencies
- `config` (for world generation params).
- `protocol` (for entity types).
