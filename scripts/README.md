# Build Scripts

Helper scripts and Make targets for building and running the project.

## Files

- `Makefile` - Build automation targets
- `build.sh` - Docker build helper script
- `test.sh` - Test execution helper script
