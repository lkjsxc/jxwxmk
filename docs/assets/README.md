# Assets

Source-of-truth assets referenced by documentation live here. These files are not served at runtime unless explicitly copied into `src/static/` during build.

## Contents

- `favicon.ico`: Provided project favicon. Copy into `src/static/` when wiring up static asset serving.
